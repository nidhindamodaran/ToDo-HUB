<% if @save_flag %>
  $("#show_tabs").prepend("<%= j(render(:partial => @task)) %>");
  $('.new_task').val('');
<% else %>
  $('#error_box').html("<%= j (render 'error_dialog') %>");
  $('#error_box').effect("shake");
<% end %>
