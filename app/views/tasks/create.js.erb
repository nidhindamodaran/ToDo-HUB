<% if @save_flag %>
  $("#show_tabs").html("<%= j(render(partial:'active_tasks')) %>");
  $('.new_task').val('');
<% else %>
  $('#error_box').html("<%= j (render 'error_dialog') %>");
  $('#error_box').fadeIn();
  setTimeout(function(){
  $('#error_box').fadeOut();
}, 1500);
<% end %>
