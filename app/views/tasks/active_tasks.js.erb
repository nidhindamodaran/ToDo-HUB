<% if params[:page].present?  %>
  $('#tasks_lists').append('<%= j render(@tasks) %>');
  <% if @tasks.next_page %>
    $('.pagination').replaceWith('<%= j will_paginate(@tasks) %>');
  <%else %>
    $('.pagination').remove();
  <%end%>
<% else %>
  $("#show_tabs").html("<%= j(render(partial:'active_tasks')) %>");
<% end %>
